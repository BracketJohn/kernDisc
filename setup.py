from setuptools import setup

setup(name='kerndisc',
      version='0.0.1',
      description='Automatic Kernel Discovery for Gaussian Processes.',
      url='https://github.com/BracketJohn/imPhysGP',
      author='BracketJohn',
      packages=['kerndisc'],
      zip_safe=False)
